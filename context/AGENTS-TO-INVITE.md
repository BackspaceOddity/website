# Recommended agents for this session

Run `/invite` and select:

- **figma-web-pixel-perfect** — primary. Next.js + verification pipeline expertise. Rule #5 (Gate-3 hook), rule #1 (verify-the-verifier) directly applicable.
- **knowledge-architect** — for any decision-trace nodes that come out of this migration. Rule #5d (cross-project flow), #3 (SSoT), #2 (enforcement-as-hook) directly relevant.
- **tone-of-voice** — guard during port. Don't accidentally rewrite locked Notion-canonical copy when re-shaping JSX.
- **typography** — tertiary. Type scale audit during port (V2 has ~13 distinct sizes, rule #2 says max 5; web context lets some slip but worth the pass).

To load all four at once: `/invite figma-web-pixel-perfect knowledge-architect tone-of-voice typography`
