node_modules/
.DS_Store
.claude/
.playwright-mcp/
bugs/
.project-journal/.session-*
.project-journal/.last-wrap-*
.project-journal/.wrap-warned-*
.project-journal/.wrap-all-lock
.project-journal/sessions/*-yegorkorobeynikov.md

# Next.js
.next/
out/
*.tsbuildinfo
.vercel
.env*.local

# Edit-mode runtime store (machine-local; gets pulled into CC sessions via hook)
_edit-threads.json
_edit-threads.processed.jsonl
.project-journal/watcher-nudges/

# Font tweaks panel — runtime inbox files (local-only, written by inbox-server.py)
_edit-inbox.json

# Per-session ephemera written by Second Brain hooks and /wrap skill (local-only by design).
# `**/` prefix matches at any depth (root + nested sub-projects).
# Canonical source: SB `_system/git-config/gitignore-project-journal-rules.txt`.
**/.project-journal/sessions/
**/.project-journal/watcher-nudges/
**/.project-journal/.session-*
**/.project-journal/.last-wrap-*
**/.project-journal/.last-end-*
# Workspace-delta cursor IS tracked (read by /resume Step 5.9).
!**/.project-journal/.last-snapshot.json
.mcp.json

# Per-client access codes — never commit
.workspace-secrets/

# One-way workshop dev fallback storage
.workspace-data/
.supabase-setup.env

# ToV lint pipeline scratch files (local, written by tov-coach pipeline)
_tov-requests.json
_tov-results.json
